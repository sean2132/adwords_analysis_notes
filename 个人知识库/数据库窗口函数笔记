# 🪟 MySQL 窗口函数（Window Functions）完整笔记（增强版）

> 🎯 目标：彻底掌握窗口函数的语法结构、参数含义、各类函数及其使用方式（含窗口范围完整细化）

---

# 一、窗口函数是什么？

## 1. 定义

👉 窗口函数：  
在**不减少行数的情况下**，对“当前行相关的一组数据”进行计算

---

## 2. 与 GROUP BY 的区别

| 特点 | GROUP BY | 窗口函数 |
|------|----------|----------|
| 行数 | 会减少 | 不变 |
| 结果 | 每组一行 | 每行都有结果 |
| 使用场景 | 汇总 | 分析 |

---

## 3. 核心理解

👉 每一行都有一个“窗口（window）”  
👉 窗口 = 与当前行相关的一组数据

---

# 二、窗口函数核心语法（必须掌握）

## ⭐ 标准语法

```sql
window_function(expression) OVER (
    [PARTITION BY ...]
    [ORDER BY ...]
    [frame_clause]
)
1. function（函数部分）
聚合函数：SUM / AVG / COUNT / MAX / MIN
排名函数：ROW_NUMBER / RANK / DENSE_RANK
分析函数：LAG / LEAD / FIRST_VALUE / LAST_VALUE
2. OVER（核心关键字）

👉 标志窗口函数

3. PARTITION BY（分区）

👉 逻辑分组（不减少行）

4. ORDER BY（排序）

👉 决定窗口内部顺序（影响累计/排名）

5. frame_clause（窗口范围）⭐️重点扩展
三、窗口范围（frame_clause）完整详解 ⭐⭐⭐
1. 基本语法
ROWS BETWEEN 起点 AND 终点
RANGE BETWEEN 起点 AND 终点
2. 边界关键字
⭐ 当前行
CURRENT ROW

👉 表示当前行

⭐ 起点 / 终点类型
（1）UNBOUNDED PRECEDING
UNBOUNDED PRECEDING

👉 从最开始的一行

（2）UNBOUNDED FOLLOWING
UNBOUNDED FOLLOWING

👉 到最后一行

（3）N PRECEDING
N PRECEDING

👉 当前行之前的第 N 行

（4）N FOLLOWING
N FOLLOWING

👉 当前行之后的第 N 行

3. 常见窗口组合
（1）累计（默认）
ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW

👉 从第一行到当前行（累计）

（2）向前看（未来数据）
ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING

👉 当前行到最后一行

（3）全窗口
ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING

👉 所有行

（4）固定滑动窗口（过去）
ROWS BETWEEN 2 PRECEDING AND CURRENT ROW

👉 当前行 + 前2行

（5）对称窗口（前后）
ROWS BETWEEN 2 PRECEDING AND 2 FOLLOWING

👉 当前行 ±2行

（6）未来窗口
ROWS BETWEEN CURRENT ROW AND 2 FOLLOWING

👉 当前行 + 后2行

4. ROWS vs RANGE
类型	含义
ROWS	按行数计算
RANGE	按值范围计算
示例对比
-- 按行
ROWS BETWEEN 2 PRECEDING AND CURRENT ROW

-- 按值
RANGE BETWEEN 100 PRECEDING AND CURRENT ROW
5. 默认行为（极重要）
情况1：有 ORDER BY
RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW

👉 默认累计

情况2：没有 ORDER BY

👉 整个分区都是窗口

四、窗口函数执行顺序
FROM → WHERE → GROUP BY → HAVING → 窗口函数 → ORDER BY
五、窗口函数分类
1️⃣ 聚合窗口函数
SUM(value) OVER (ORDER BY date)

👉 累计求和

2️⃣ 排名函数
ROW_NUMBER()
ROW_NUMBER() OVER (ORDER BY score DESC)
RANK()

👉 会跳号（如：1,1,3）

DENSE_RANK()

👉 不跳号（如：1,1,2）

3️⃣ 分析函数
LAG()
LAG(value, 1)

👉 前一行

LEAD()

👉 后一行

FIRST_VALUE / LAST_VALUE

👉 注意窗口范围影响结果

六、移动计算
移动平均
AVG(value) OVER (
    ORDER BY date
    ROWS BETWEEN 2 PRECEDING AND CURRENT ROW
)
累计求和
SUM(value) OVER (ORDER BY date)
七、排名与分布
PERCENT_RANK()
PERCENT_RANK() OVER (ORDER BY score)
CUME_DIST()
CUME_DIST() OVER (ORDER BY score)
八、同比 / 环比
环比
value - LAG(value, 1) OVER (ORDER BY date)
同比
value - LAG(value, 12) OVER (ORDER BY month)
增长率
(value - LAG(value)) / LAG(value)
九、WINDOW 子句
WINDOW w AS (PARTITION BY category ORDER BY date)
十、关键总结
核心结构
函数 + OVER(分区 + 排序 + 范围)
三大控制
PARTITION BY
ORDER BY
FRAME
窗口范围核心记忆

👉 本质：

PRECEDING = 过去
FOLLOWING = 未来
CURRENT ROW = 当前
🚀 一句话总结

👉 窗口函数本质：

每一行，在“自己的一段数据范围内”做计算
